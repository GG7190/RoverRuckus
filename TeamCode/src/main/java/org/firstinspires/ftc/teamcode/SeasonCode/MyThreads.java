package org.firstinspires.ftc.teamcode.SeasonCode;

/*public class MyThreads extends Thread {

    private GGHardware _robot = null; //Change the time to match what is in the code

    public void setRobotHardware(GGHardware robot)
    {
        _robot = robot;
    }

    private boolean doStop = false;

    public synchronized void Stop()
    {
        this.doStop = true;
    }

    public void run()
    {
        string threadName = Thread.currentThread().getName();
        while(keepRunning())
        {
            if(_robot !=  null)
            {
                _robot.SendMessage(threadName + "-I am alive..."); //change the function to match code
            }
            Thread.sleep(3L *1000L);
        }
        if(_robot != null )
        {
            _robot.SendMessage(threadName + "-Terminating...");//change the function to match the code
        }
    }


}*/
